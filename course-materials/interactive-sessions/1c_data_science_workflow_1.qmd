---
title: "Interactive Session"
subtitle: "🎬 The Python Data Science Workflow, Part 1: Import to Aggregate"
jupyter: eds217_2026
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: show
---

This afternoon we run a complete data science workflow, from a raw file sitting on the internet
to a monthly climate summary of Arctic air temperature. We run through all of it once today, and
over the next six days (Days 2 through 7) we will learn how each part of it was built.

::: {.callout-important title="Don't panic 🚀"}
You are not expected to understand every line today. Your job is to copy code, run it, and
change one thing at a time. Our job (that's Cella and Kelly) is to show you what a whole
workflow looks like from one end to the other. By the end of Day 7 you should know how all of
it works.
:::

We will use real Arctic weather data from the [Arctic LTER](https://lternet.edu/site/arctic-lter/)
station at Toolik Lake, Alaska: daily measurements from 1988 onward. Part 1 takes one path through
the ten steps: Import, Explore, Group and Aggregate, which are steps 1, 2, 7 and 8. We skip
Clean, step 3, because the one column we work with today has no missing values, though other
columns in the file do have gaps. Session 1D picks up with Visualize and
Export, and names all ten steps of the workflow we are working through.

## Getting Started

Set up your notebook with the usual ritual:

1. **Create the file.** In the Explorer, hover over the **EDS217** heading and click
   **New File...**, then type the name in full, extension included:
   `Session_1C_Workflow_1.ipynb`

2. **Check the kernel.** The Kernel Selector in the notebook's action bar should read
   **Python 3.11.15 (Conda: eds217)**. If it reads anything else, click it, choose
   **Change Kernel**, and pick the eds217 entry.

3. **Add a title cell.** Click **+ Markdown** in the action bar, and give it this
   content, with today's date:

```markdown
# Day 1: Session 1C - The Python Data Science Workflow, Part 1

[Session Webpage](https://eds-217-essential-python.github.io/course-materials/interactive-sessions/1c_data_science_workflow_1.html)

Date: 08/31/2026
```

4. **Save** with `Ctrl + S` (`Cmd + S` on macOS), and keep saving as you go.

:::{.callout-caution}
Save often (`Ctrl+S` / `Cmd+S`).
:::

## How today's steps work

This afternoon we run code you cannot fully read yet. To make that manageable, every step of
the workflow follows the same small pattern. We chose this pattern for Day 1 to help you build
confidence before you build understanding.

Each step has up to three parts:

1. **Run it.** Copy the canonical cell and run it exactly. Everyone gets the same result, so we
   all begin each step from the same place.
2. **Change one thing.** Swap a single item from a short menu, then run it again to see what
   moves. Each of these cells rebuilds its result from `df`, so they leave your data alone. The
   next canonical cell brings everyone back together.
3. **Say it in a sentence.** After some steps you will write one f-string that reports a number
   you just produced. We learned f-strings this morning, so these are the places where you get
   to practice them on numbers you produced yourself.

Our goal on Day 1 is to see the whole workflow and get a feel for what each step is for, and
mastering the syntax comes later. Running working code shows you what each step does before you
have to write it yourself, changing one thing gives you a safe way to experiment without
breaking your data, and writing a sentence about the result gives you some practice with the one
skill you already have.

## Step 1: Import 📂

**Import** means loading your tools and then loading your data.

✅ **Canonical cell. Copy and run it exactly:**

```{python}
import pandas as pd
import matplotlib.pyplot as plt

url = "https://eds-217-essential-python.github.io/data/toolik_weather.csv"
df = pd.read_csv(url)
```

The `import ... as ...` lines load Python toolkits under short nicknames. Two of them run today: `pd` for pandas (data) and `plt` for matplotlib
(plots). A third, `np` for numpy (math), turns up later in the course. `pd.read_csv(url)` reads a CSV straight from the web into a **DataFrame**, a table,
which we store in `df`.

:::{.callout-note}
Today `read_csv()` needs only one thing: the `url` of a CSV file. It can do a great deal more,
and we will get to the rest of it on Day 2.
:::

:::{.callout-tip title="🔁 Change one thing"}
`read_csv` builds a table, and you can give that table any name you like. Load the same data
into a new variable, using one name from this menu:

- `climate = pd.read_csv(url)`
- `toolik = pd.read_csv(url)`
- `weather = pd.read_csv(url)`

Your original `df` stays exactly as it was. You have simply made a second copy under a new name.
:::

```{python}
# 🔁 Example: same data under a new name. Reads url again; leaves df alone.
climate = pd.read_csv(url)
```

:::{.callout-tip title="✍️ Say it in a sentence (optional)"}
Write an f-string that names what you loaded. For example, set `dataset = "Toolik weather"` and
then print `f"I loaded the {dataset} dataset."`
:::

## Step 2: Explore 🔎

**Explore** means getting to know the table before you analyze it.

✅ **Canonical cell. Preview the first rows:**

```{python}
df.head()
```

:::{.callout-tip title="🔁 Change one thing"}
Run `df.info()`, which the required sentence below needs, then try one other no-argument
view of the same table:

- `df.info()` shows the column names, types, and how many values are present.
- `df.describe()` shows summary statistics for the number columns.

Each one looks at `df` without changing it.
:::

```{python}
# 🔁 Example: a structural overview instead of a row preview. Reads df; leaves it alone.
df.info()
```

```{python}
# 🔁 Example: summary statistics. Reads df; leaves it alone.
df.describe()
```

### The health check

Before you trust a dataset, run a quick **health check**. The health check is one line of code
that you will reuse all course long, and it counts the missing values in every column:

```{python}
df.isnull().sum()
```

:::{.callout-important}
Look at `Daily_AirTemp_Mean_C`. It has zero missing values, so the column we are about to
analyze is complete. A few other columns do have gaps, which is normal. We only need our column
to be clean.
:::

:::{.callout-important title="✍️ Say it in a sentence (required)"}
`df.info()` reported how many rows the dataset has. Read that number, then report it in your own
f-string. For example:

```python
n_rows = 11171          # the number info() showed you
print(f"The Toolik dataset has {n_rows} daily records.")
```

Now write a second sentence of your own, maybe about how many columns there are.
:::

## Step 3: Clean 🧼, which we skip today

The next step in a normal workflow is **Clean**: fixing missing values, wrong types, and
duplicate rows. Our health check already showed that `Daily_AirTemp_Mean_C` has no missing
values, so we have nothing to clean today. Today's path now jumps to step 7, because steps 4,
5 and 6, Filter, Sort and Transform, are not ones this analysis needs.

:::{.callout-note}
Enjoy this while it lasts. Most real datasets need real cleaning, and it is often the biggest
part of a project. We spend Day 4 on cleaning and transforming. Today the data came ready to use.
:::

## Step 7: Group 🗂️

**Group** means splitting the rows into buckets by some key. Here we bucket every daily record
by its **Month**, so all the Januaries sit together, all the Februaries, and so on.

✅ **Canonical cell. Copy and run it exactly:**

```{python}
monthly = df.groupby('Month')
```

:::{.callout-tip title="🔁 Change one thing"}
Change only the key you group by. Pick one from this menu:

- `by_year = df.groupby('Year')` buckets records by year, which gives many groups.
- `by_station = df.groupby('Station')` gives a single bucket here, because every row is the
  same station.

Both read `df` without changing it, and neither touches your `monthly` grouping.
:::

```{python}
# 🔁 Example: group by Year instead of Month. Reads df; leaves df and `monthly` alone.
by_year = df.groupby('Year')
```

## Step 8: Aggregate 📊

**Aggregate** means collapsing each bucket down to a single number. Here we take the mean
temperature within each month. Notice the two-step pattern: first group, then pick a column and
average it.

✅ **Canonical cell. Copy and run it exactly:**

```{python}
monthly_means = monthly['Daily_AirTemp_Mean_C'].mean()
monthly_means
```

The result is twelve numbers, one average temperature per month, which summarizes thousands of
daily records into a clear seasonal pattern.

:::{.callout-tip title="🔁 Change one thing"}
Keep the grouping and change only the column you average. Pick one from this menu:

- `monthly['Daily_Precip_Total_mm'].mean()` gives average daily precipitation.
- `monthly['Daily_AirTemp_AbsMax_C'].mean()` gives the average daily high temperature.

Store the result under a new name so your `monthly_means` stays put.
:::

```{python}
# 🔁 Example: average a different column. Reads `monthly`; leaves monthly_means alone.
monthly_precip = monthly['Daily_Precip_Total_mm'].mean()
monthly_precip
```

:::{.callout-important title="✍️ Say it in a sentence (required)"}
Look at your `monthly_means`, pick one month's value, read it off, and report it in an f-string.
For example:

```python
july_temp = 11.2        # July's value, read from monthly_means
print(f"At Toolik, the average July temperature is {july_temp} degrees Celsius.")
```

Now write one more sentence for the month you find most surprising.
:::

## 🧪 Sandbox (5 minutes)

Now poke at it yourself. Try a different column, a different grouping key, or a deliberate typo.
Errors are expected today, and they are welcome: a red error message reports which line Python
could not run and what went wrong with it, so read the message before you change anything. See what you
can make happen!

:::{.callout-caution title="↩️ Canonical restart. Run this to rejoin the group"}
When the five minutes are up, run this cell to reload the data and rebuild the canonical
results. Everyone returns to the same state, ready for Part 2:

```{python}
# Restart: reload df and rebuild the canonical results
df = pd.read_csv(url)
monthly = df.groupby('Month')
monthly_means = monthly['Daily_AirTemp_Mean_C'].mean()
```
:::

## Key Points

- We ran the first four steps of a real path through the workflow: Import, Explore, Group and
  Aggregate, which are steps 1, 2, 7 and 8. We skipped Clean because the health check showed
  that our temperature column was complete, and skipped Filter, Sort and Transform because this
  analysis does not need them.
- `pd.read_csv(url)` loads a CSV from the web into a DataFrame. `.head()`, `.info()`, and
  `.describe()` are your no-argument ways to look at it.
- `df.isnull().sum()` is the health check for counting missing values.
- Grouping and then averaging turns thousands of rows into a short summary. We did it in two
  steps: first `monthly = df.groupby('Month')`, then `monthly['Daily_AirTemp_Mean_C'].mean()`.
- The change-one-thing cells rebuild their result from a table that already exists rather than
  editing one, so your data stays put and we can all get back to the same results.

## Resources

- [Course Cheatsheet: `read_csv`](../cheatsheets/read_csv.qmd)
- [Course Cheatsheet: Workflow Methods](../cheatsheets/workflow_methods.qmd)
- [Course Cheatsheet: Data Grouping](../cheatsheets/data_grouping.qmd)

➡️ **Next:** In [The Python Data Science Workflow, Part 2](1d_data_science_workflow_2.qmd) you will
visualize these monthly means, save your results to a file, and see all ten steps of the
workflow laid out in order.

::: {.center-text .body-text-xl .teal-text}
End interactive session 1C
:::
